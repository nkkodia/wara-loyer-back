# application.yml (fichier principal)
spring:
  profiles:
    active: prod
  mail:
    host: smtp.gmail.com  # Le serveur SMTP de votre fournisseur
    port: 587                      # 587 (TLS) ou 465 (SSL)
    username: waraloyer@gmail.com
    password: uvrlsvovsunqydwi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
management:
  # 1. RETIRER LA LIGNE BASE-PATH. Les endpoints reviennent à /actuator
  endpoints:
    web:
      exposure:
        include: prometheus, health # Incluez explicitement les endpoints
      # RETIREZ base-path: /management

  # 2. Le reste est bon
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    prometheus:
      enabled: true